
//Que es un elemento 
//Es una etiqueta html que se puede manipular desde javascript
/*Div es un elemento*/
/*Un elemento no deja de ser un etiqueta pero los atributos son nodos.*/

//que es un atributo
//Es un elemento que se puede manipular desde javascript 
/*Es un atributo de un elemento*/

//que es un nodo 
//Es un elemento que se puede manipular desde javascript
/*El nodo seria un comentario.*/

//todos los nodos son elementos pero no todos los elementos son nodos? 
//No, todos los nodos son elementos pero no todos los elementos son nodos.

//que es un selector
//Es una forma de seleccionar un elemento del DOM

//que es el DOM? Una jerarquia de elementos.
//Es una estructura de datos que representa el documento html
/*Es un arbol de nodos*/

//que es un evento
//Es una accion que ocurre en el DOM y que puede ser detectada por javascript
/*Es un click, un scroll, un resize, etc*/

//que es un formulario
//Es un elemento que permite enviar datos al servidor web a traves de una peticion http y que puede ser manipulado desde javascript 

//que es un metodo
//Es una funcion que pertenece a un objeto y que puede ser invocada desde javascript


// Un atributo de document es FORM, que es un elemento que permite enviar datos al servidor web a traves de una peticion http y que puede ser manipulado desde javascript
// Un atributo de document es getElementById, que es un selector que permite seleccionar un elemento del DOM
// Un atributo de document es getElementsByClassName, que es un selector que permite seleccionar un elemento del DOM
// Un atributo de document es getElementsByTagName, que es un selector que permite seleccionar un elemento del DOM
// Un atributo de document es querySelector, que es un selector que permite seleccionar un elemento del DOM
// Un atributo de document es querySelectorAll, que es un selector que permite seleccionar un elemento del DOM
// Un atributo de document es title, que es un atributo de un elemento
// Un atributo de document es URL, que es un atributo de un elemento
// Un atributo de document es body, que es un elemento
// Un atributo de document es head, que es un elemento
// Un atributo de document es forms, que es un elemento
// Que es getElementsByClassName? 
// Es un selector que permite seleccionar un elemento del DOM por su clase de css 


//que nodos tiene un textarea?
//un textarea tiene nodos de tipo text, password, email, etc.
//que nodos tiene un button?
//un button tiene nodos de tipo text, password, email, etc.
//que nodos tiene un label?
//un label tiene nodos de tipo text, password, email, etc.
//que nodos tiene un select?
//un select tiene nodos de tipo option, etc.
//que nodos tiene un option?
//un option tiene nodos de tipo text, password, email, etc. 
//que nodos tiene un fieldset?
//un fieldset tiene nodos de tipo legend, etc.